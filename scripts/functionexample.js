function hello(firstName, lastName) {
  var fullName = firstName + " " + lastName;
  return console.log("Hello " + fullName);
}

hello("Jeffrey", "West");
hello("Martin", "Reynolds");
hello("Marshall", "Mathers");           
